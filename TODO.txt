create bin patcher to replace app launcher "asmv3:file name='"
add settings button
user can view and edit weapons/materials stats
user can swap decimal place indicator from period(.) to comma(,)
user can hide columns for weapons with ratio == 0